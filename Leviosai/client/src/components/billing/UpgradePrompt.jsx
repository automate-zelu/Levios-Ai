/**
 * Legacy near-limit banner. Commercial pricing has no lead/minute caps,
 * so this never renders. File kept for existing imports / inventory tests.
 */
export function UpgradePrompt() {
  return null;
}
